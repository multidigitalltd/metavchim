node_modules/
dist/
dist-esm/
.next/
.turbo/
coverage/
*.tsbuildinfo
.env
.env.local
.env.*.local
.env.production
backups/
*.log
.DS_Store

# תמונת מצב של Redis — נכתבת לתיקייה שממנה הורץ redis-server בפיתוח.
# נתוני ריצה מקומיים, לא מקור.
dump.rdb

# פסולת של מפרש פייתון (שירותי infra/stt ו-infra/diarize)
__pycache__/
*.pyc

# ה-Worker של MapLibre, שמועתק ל-public בכל בנייה ובכל הרצת פיתוח
# (apps/web/scripts/copy-maplibre-worker.mjs). נוצר מ-node_modules ולכן
# אינו מקור: עותק שנשמר ב-git היה נפרד בשקט מהגרסה המותקנת.
apps/web/public/maplibre/

# תצורת הפריסה שמולאה — כתובת שרת ייצור, וסודות מוצפנים או לא
infra/ansible/inventory.ini
infra/ansible/group_vars/all.yml
infra/ansible/group_vars/vault.yml
