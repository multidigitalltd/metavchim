-- העדפות אישיות של המשתמש, שנוסעות איתו בין מכשירים.
--
-- עד כה הן ישבו ב-localStorage בלבד. הנחת המוצא הייתה שהן תלויות
-- מכשיר (גודל מסך, עכבר), אבל בפועל מתווך שמגדיל פונט או מדליק
-- ניגודיות עושה זאת בגלל *העיניים שלו*, לא בגלל המסך — והוא ציפה
-- למצוא את ההגדרה גם בנייד וגם במחשב במשרד.
--
-- עמודת JSON כללית ולא עמודה לכל דגל: העדפות אישיות נוספות כבר
-- בדרך, וכל אחת מהן לא צריכה מיגרציה משלה. אין כאן PII.
ALTER TABLE users ADD COLUMN preferences JSONB NOT NULL DEFAULT '{}';
