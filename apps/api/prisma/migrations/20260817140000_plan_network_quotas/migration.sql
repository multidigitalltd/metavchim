-- מכסות פרסום ברשת השיתופים ברמת המסלול.
--
-- NULL = ללא הגבלה, וזה גם ברירת המחדל של העמודות. כלומר כל מסלול
-- שכבר שמור בטבלה ממשיך בדיוק כפי שהיה, ואף משרד קיים אינו נחסם
-- מפרסום בגלל ההגירה הזו. המכסות נכנסות לפעולה רק במסלול שמישהו
-- קבע בו מספר — היום זה מסלול השת"פ החינמי בלבד.
ALTER TABLE "plans"
  ADD COLUMN "max_network_listings" INTEGER,
  ADD COLUMN "max_network_demands" INTEGER;
