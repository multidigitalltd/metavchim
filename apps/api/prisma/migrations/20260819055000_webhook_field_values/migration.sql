-- ערכים ולא רק שמות, לשדות הטכניים ביומן הוובהוק.
--
-- לדעת ש-`recording` הגיע אינו מספיק כדי לבנות מולו — צריך לראות
-- את **הצורה** של הערך. רשימת היתר בקוד (`diagnosticFields`) מגבילה
-- את זה לשדות טכניים בלבד: נתיב הקלטה, סטטוס, כיוון, מזהים וזמנים.
-- מספרי טלפון ושמות לקוחות ממשיכים להישמר כשם השדה בלבד.
ALTER TABLE telephony_webhook_hits
  ALTER COLUMN field_keys TYPE VARCHAR(1000);
