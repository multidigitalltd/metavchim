-- השדות שהספק שולח ואנחנו מתעלמים מהם.
--
-- "אילו שדות הגיעו" עונה על חצי שאלה. החצי שחסר הוא מה מתוכם
-- נבלע — כלומר איפה בדיוק יושב מידע שאנחנו לא מנצלים. בלי זה,
-- הוספת שם חדש לרשימת השמות היא ניחוש מתוך שלושה-עשר שדות.
--
-- עמודה נפרדת ולא חלק מ-field_keys: זו השאלה שצריכה לקפוץ לעין,
-- ומיזוגה לרשימה הכללית היה מטשטש אותה.
ALTER TABLE telephony_webhook_hits ADD COLUMN unmapped_fields VARCHAR(500);
