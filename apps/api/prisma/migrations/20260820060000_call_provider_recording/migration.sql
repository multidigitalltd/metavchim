-- נתיב ההקלטה אצל ספק המרכזייה.
--
-- עמודה נפרדת מ-`recording_key` בכוונה: זה מפתח S3 שלנו, ומסלולי
-- מחיקת המידע סורקים אותו ומוחקים את האובייקטים המתאימים. נתיב של
-- ספק חיצוני שם היה גורם למחיקה „להצליח” על אובייקט שאינו קיים —
-- כלומר המערכת מדווחת שההקלטה נמחקה בזמן שהאודיו עדיין אצל הספק.
ALTER TABLE calls ADD COLUMN IF NOT EXISTS provider_recording_path VARCHAR(300);
