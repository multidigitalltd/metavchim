-- הסוכן שפרסם את הנכס לרשת.
--
-- תנאי חלוקת העמלה הם התחייבות כלפי משרדים אחרים, ועד עכשיו כל סוכן
-- במשרד יכול היה לשנות אותם — או להוריד את הפרסום — של עמית, כי
-- השליפה סוננה לפי `tenant_id` בלבד.
--
-- אצל הקונה יש כבר בעלים (`buyers.owner_user_id`), ולכן שם הכלל
-- נגזר ממנו. לנכס אין בעלים ברמת הסוכן — נכסים גלויים לכל המשרד
-- בכוונה — ולכן הבעלות על **תנאי הפרסום** היא של מי שקבע אותם.
--
-- NULL = פרסום שקדם לעמודה. אלה נשארים ניתנים לעריכה בידי מנהל
-- בלבד, ולא נפתחים לכל המשרד: פרסום ישן אינו סיבה לוותר על הכלל.
ALTER TABLE shared_listings ADD COLUMN IF NOT EXISTS created_by CHAR(26);
