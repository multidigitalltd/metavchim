-- גישה לסוכן האישי בוואטסאפ — מנוי נפרד לכל סוכן.
--
-- ברירת המחדל FALSE במכוון: הגישה היא תוספת בתשלום שמפעיל בעל
-- המשרד לכל סוכן בנפרד (מסך ניהול משרד → סוכני המשרד). בעל המשרד
-- עצמו כלול תמיד — האכיפה בקוד הסוכן, לא כאן.

-- AlterTable
ALTER TABLE "users" ADD COLUMN "whatsapp_access" BOOLEAN NOT NULL DEFAULT false;
