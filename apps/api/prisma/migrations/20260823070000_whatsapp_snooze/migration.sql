-- „שקט לשעתיים” — ההשתקה הרגעית שהכפתור בהודעה מפעיל.
--
-- ספרייה חדשה ולא הרחבה של המיגרציה הקודמת: זו כבר הורצה בפרודקשן
-- ורשומה כמיושמת, ו-`prisma migrate deploy` מריץ רק מה שממתין —
-- כלומר תוספת בתוכה לא הייתה רצה לעולם, והקוד היה שואל על עמודה
-- שאינה קיימת (ביקורת Codex).

-- AlterTable
ALTER TABLE "whatsapp_chats" ADD COLUMN "notify_snooze_until" TIMESTAMP(3);
