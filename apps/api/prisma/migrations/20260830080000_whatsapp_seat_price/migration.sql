-- מחיר לסוכן וואטסאפ נוסף — **לכל מסלול בנפרד.**
--
-- מספר גלובלי אחד היה מחייב את אותו מחיר בכל המסלולים, ובדיוק זה
-- מה שאין: מסלול בסיסי יכול בכוונה לא למכור מקומות נוספים, ומסלול
-- גבוה יכול למכור אותם בזול. `NULL` = לא נמכר, וזה מצב תקין.
ALTER TABLE "plans"
  ADD COLUMN "whatsapp_seat_monthly_agorot" INTEGER;
