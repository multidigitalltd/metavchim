-- מקור השורה: נשכר מהמלאי של 015 דרך דף תשלום, או חיוב שמנהל
-- הפלטפורמה פתח על מספר שכבר בידי המשרד (למשל ממרכזייה משלו).
--
-- ‎`platform` מחויב מדי חודש בדיוק כמו השכרה — אותו סורק, אותו כרטיס
-- שמור — אבל אין בו תפיסה ואין שחרור אצל 015, ושחרור השורה מפסיק
-- חיוב בלבד ואינו מכבה את הניתוב של המספר: המספר של המשרד הוא.
--
-- ברירת המחדל `purchased` היא הנכונה לשורות הקיימות: עד היום כל
-- שורה כאן נוצרה מדף תשלום.
ALTER TABLE "rented_numbers"
  ADD COLUMN "origin" VARCHAR(10) NOT NULL DEFAULT 'purchased';
