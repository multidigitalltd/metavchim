-- משוב מביקור — מה אמר הקונה, בשלוש הקשות (docs/03 — appointments).
--
-- ‏עד היום הסיור נסגר ב„תוצאה” אחת (אהב / לא מתאים / מו"מ / צריך
-- ‏אחר) — נכון לקונה, ולא אומר דבר למוכר. מה שמוריד מחיר בלי ויכוח
-- ‏הוא „שישה מתוך שמונה אמרו שהמחיר גבוה”, וזה דורש שאלות סגורות
-- ‏שנספרות: מחיר, מצב, התאמה.
--
-- ‏שלוש עמודות על הפגישה ולא טבלה: המשוב הוא של הסיור (1:1), נשאל
-- ‏פעם אחת, ונקרא יחד איתו בכל מקום שהסיור נקרא. הרשימות סגורות
-- ‏בקוד (`VIEWING_*_FEEDBACK` בחבילה המשותפת). ‎`notes` הקיים נשאר
-- ‏המשפט החופשי הפנימי — הוא אינו יוצא למוכר לעולם.

ALTER TABLE "appointments"
  ADD COLUMN "feedback_price" VARCHAR(12),
  ADD COLUMN "feedback_condition" VARCHAR(12),
  ADD COLUMN "feedback_fit" VARCHAR(12);
