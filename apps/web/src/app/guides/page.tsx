import { permanentRedirect } from "next/navigation";

/**
 * ‎**„הדרכות” אינו מסך יותר — הוא הפניה לתיעוד הציבורי.**
 *
 * ההדרכות והתיעוד היו אותו תוכן בשני בתים: `/guides` מאחורי
 * התחברות, ו-`/docs` פתוח. שני בתים לאותו תוכן פירושם שני מסכים
 * לתחזק, ושתי חוויות שנפרדות ביום שאחד מהם משתפר.
 *
 * הבית שנשאר הוא **הציבורי**, כי הוא היחיד ששלושת הקהלים
 * שאינם מחוברים יכולים להגיע אליו: מי ששוקל להצטרף, מי שמחבר
 * מערכת אחרת, ומודל שפה שקורא את העמוד. מי שכבר בפנים לא מפסיד
 * דבר — הלשונית בתפריט מובילה לאותו מקום.
 *
 * ## למה הנתיב נשאר ולא נמחק
 *
 * ‎`/guides` ו-`/guides/<נושא>` מקושרים מתוך המערכת, מהודעות
 * שנשלחו, ומסימניות של מתווכים. מחיקה הייתה הופכת כל אחד מהם
 * ל-404. הפניה קבועה (308) אומרת לדפדפן ולמנוע החיפוש שהכתובת
 * עברה — ולכן היא מתקנת את הסימנייה במקום לשבור אותה.
 */
export default function GuidesRedirect(): never {
  permanentRedirect("/docs");
}
