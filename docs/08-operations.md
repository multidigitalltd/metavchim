# 08 · תפעול ותחזוקה

## 1. סביבות

| סביבה | מטרה | דאטה |
|-------|------|------|
| Local | פיתוח (Docker Compose: PG, Redis, Mailpit, MinIO, Fake Providers) | סינתטי (Seeders) |
| Staging | בדיקות אינטגרציה, ספקי Sandbox (WhatsApp Test, טלפוניה) | סינתטי — **לעולם לא דאטה אמיתי** |
| Production | לקוחות | אמיתי, מוצפן |

- אחסון: ענן עם ריג'ן אירופי קרוב (זמן תגובה לישראל) או ספק ישראלי — לפי דרישות לקוחות Enterprise לגבי מיקום דאטה.
- כל התשתית כ-IaC (Terraform) — סביבה ניתנת לשחזור מלא מקוד.

## 2. CI/CD

Pipeline לכל PR (חוסם מיזוג):
1. Lint + Static Analysis (TypeScript strict, ESLint + כללי אבטחה, dependency-cruiser לגבולות מודולים)
2. בדיקות: Unit (פר-מודול) → Feature → **סוויטת Cross-Tenant** → Architecture tests (גבולות מודולים)
3. סריקת אבטחה: תלויות (pnpm audit), סודות, SAST (Semgrep)
4. נגישות: axe-core על מסכי מפתח; Lighthouse CI (תקציבי ביצועים)
5. Build + מיגרציות על DB נקי

Deploy: מיזוג ל-main → Staging אוטומטי → Production בקליק (עם אישור), Zero-Downtime (מיגרציות תואמות-לאחור בשני שלבים: Expand → Migrate → Contract), Rollback בפקודה אחת. Feature Flags מנתקים "פריסה" מ"הפעלה".

## 3. ניטור והתראות

| רובד | כלי | מה נמדד |
|------|-----|---------|
| APM | Sentry + APM (או מקבילה) | שגיאות, טרנזקציות איטיות, N+1 |
| תשתית | Prometheus/Grafana או ניטור ענן | CPU/RAM/דיסק/חיבורי DB |
| תורים | BullMQ Dashboard (Bull Board) + התראות | עומק תור, Jobs כושלים, זמן המתנה |
| עסקי-תפעולי | דשבורד פנימי | שיחות שנכשלו, Webhooks שנדחו, תבניות שנחסמו, Quality Rating של וואטסאפ, צריכת קרדיטים חריגה |
| Uptime | בדיקה חיצונית | API, דפי הצעה, Webhook endpoints |

- התראות מדורגות: Critical → טלפון/PagerDuty; Warning → ערוץ צוות. כל התראה עם Runbook מקושר.
- **ניטור פר-דייר**: תקלה של דייר בודד (מספר וואטסאפ נחסם, יומן מנותק) מוצגת גם לדייר עצמו במסך "בריאות החיבורים" — חוסך פניות תמיכה.

### מה מוגדר היום (התקנה Standalone)

הטבלה למעלה היא היעד. זה מה שקיים בפועל ומה שצריך לחבר לפני משרד ראשון:

| נקודה | כתובת | מה נבדק |
|-------|-------|---------|
| חיים | `GET /api/v1/health` | התהליך עונה. משמש את `healthcheck` של Docker |
| עומק | `GET /api/v1/health/deep` | מסד הנתונים **ו-Redis** עונים. `503` עם שם הרכיב שנפל |

**למה Redis בבדיקת העומק**: בלעדיו אין תורים, אין התראות, אין תזכורות ואין קוד כניסה לאימייל. המערכת נראית תקינה למי שפותח מסך, ובשקט מפסיקה לעשות את העבודה שבגללה קנו אותה — בדיקה שמחזירה "תקין" במצב הזה גרועה מהיעדר בדיקה.

**להגדיר לפני העלייה לאוויר** (5 דקות, שירות חינמי כמו UptimeRobot / Better Stack):

1. ניטור על `https://app.metavchim.co.il/api/v1/health/deep` — כל 5 דקות, התראה אחרי 2 כשלים רצופים.
2. ניטור על דף ההצעה הציבורי (`/offer/…`) — זה המסלול שלקוח קצה רואה, והוא יכול להישבר בלי שהמערכת הפנימית תרגיש.
3. ניטור תוקף תעודת ה-TLS — התראה 14 יום לפני פקיעה.
4. יעד ההתראה: SMS או וואטסאפ, לא רק דוא"ל. מייל על נפילה בשבת בערב נקרא בשני בבוקר.

## 4. גיבויים ו-DR

- PostgreSQL: גיבוי מלא יומי + WAL רציף → שחזור לנקודת זמן (PITR). שמירה: 30 יום.
- S3: Versioning + רפליקציה בין-אזורית להקלטות ומסמכים.
- בדיקת שחזור **מתורגלת אחת לרבעון** (גיבוי שלא שוחזר = לא קיים).
- יעדים: RPO ≤ 15 דקות, RTO ≤ 4 שעות.
- Runbook אסון כתוב: מי מכריז, מה הסדר, איך מתקשרים ללקוחות.

## 5. תחזוקה שוטפת

- עדכוני תלויות: אוטומטיים (Dependabot) עם CI מלא; חלון תחזוקה חודשי לעדכוני תשתית.
- ניקויים מתוזמנים: Transients/טוקנים שפגו, הקלטות מעבר ל-Retention, ארכוב לידים ישנים, פרטישנים חדשים לטבלאות אירועים.
- Migrations דיסציפלינה: קדימה-בלבד, קטנות, הפיכות כשאפשר.
- תיעוד חי: כל מודול עם README (אחריות, אירועים, החלטות); Runbooks לכל תרחיש תפעולי; OpenAPI מתוחזק אוטומטית.

## 6. Onboarding דייר חדש (תהליך תפעולי-מוצרי)

אשף מובנה: פרטי משרד → הזמנת סוכנים → חיבור מספר וואטסאפ (תהליך מונחה מול Meta) → הפניית שיחות לסוכן הקולי (הנחיות פר-ספק סלולר) → ייבוא דאטה קיים (CSV/אקסל של נכסים וקונים עם מיפוי שדות חכם) → הגדרות שיתוף. יעד: משרד עובד תוך שעה, בלי איש תמיכה.

## 7. תמיכה ותפעול לקוחות

- Impersonation מבוקר: תמיכה יכולה "להיכנס כמו" משתמש רק באישורו, לזמן קצוב, עם Audit מלא.
- ייצוא דייר: כל הדאטה שלו בפורמט פתוח (CSV+JSON) בלחיצה — אמון + דרישה חוזית.
- סטטוס-פייג' ציבורי לתקלות רוחביות.

### שולחן התמיכה — מי יודע שנכנסה פנייה

תור אחד לשני מקורות הפניות (הכפתור שבמערכת, וכל כתובת בדומיין
התמיכה), ב-`/platform` בלשונית "תמיכה".

**ההתראה יוצאת בכל מסלול כניסה**, ולא רק באחד:

| מה קרה | מי מקבל |
| --- | --- |
| פנייה מהכפתור שבמערכת | כל `PLATFORM_ADMIN_EMAILS` + `supportEmail` |
| מייל חדש לכתובת התמיכה | כל `PLATFORM_ADMIN_EMAILS` + `supportEmail` |
| תשובת לקוח בשרשור קיים | אותו הדבר |
| תשובה במייל על פנייה מהכפתור | אותו הדבר |

הרשימה מאוחדת: כתובת שמופיעה גם כמנהל וגם כ-`supportEmail` מקבלת
עותק אחד. כישלון שליחה נרשם ביומן ואינו מפיל את קליטת הפנייה — היא
כבר נשמרה.

**מסירה חוזרת אינה התראה שנייה.** הספק מוסר שוב על כל תשובה שאינה
2xx, והמזהה שלו (`provider_message_id`) ייחודי בשתי טבלאות ההודעות —
‏`support_messages` ו-`support_ticket_messages`. בטבלה השנייה הוא נוסף
רק ב-`20260830210000_ticket_message_dedupe`: עד אז מסירה חוזרת של
תשובת לקוח על פנייה מהכפתור **כפלה את ההודעה על הפנייה**.

**ההתראה אינה ערוץ תשובה.** היא אינה נושאת `replyTo`, ואומרת זאת
במפורש בגוף ההודעה. תשובה של מנהל במייל לא הייתה מגיעה לפנייה אלא
פותחת שרשור חדש על שמו: הצמדה לפי מספר הפנייה דורשת שכתובת השולח
תהיה של הפונה המקורי. התשובה ללקוח נכתבת בשולחן, שם היא נרשמת עם
מצב שליחה וצירופים.

לפני התיקון הזה: פנייה מהכפתור הגיעה לכתובת אחת בלבד (וכשלא הייתה
מוגדרת — לאיש), ופנייה שהגיעה במייל לא הפעילה שום התראה. שער מבני
(`support-send-gates.test.ts`) אוכף שכל מסלול קליטה קורא ל-`notifyDesk`,
כדי שהמסלול החמישי שייוולד לא ייוולד שקט.

### תזכורות הפעלה — למי שנרשם ולא השאיר כרטיס

חשבון בניסיון שאין בו כרטיס אשראי תקף ננעל בסופו ממילא
(`tenantPeriodEnded` ⟵ `billingOnly`), ועד עכשיו הוא ננעל **בשקט**.
‏`ActivationNudgeService` שולח שלוש הודעות מדורגות:

| שלב | מתי | מה אומר |
| --- | --- | --- |
| `heads_up` | יומיים לפני סוף הניסיון | מה ייסגר, ושהנתונים נשמרים |
| `closing` | ביום שבו הניסיון נגמר | מה נסגר עכשיו, ומה נשאר פתוח |
| `last_call` | שבוע אחרי | הודעה אחרונה, ואז שקט |

- **ניסיונות בלבד** (`status = "trial"`). משרד שכבר שילם וכרטיסו פג הוא
  מקרה של גבייה שנכשלה — זרימה אחרת, נוסח אחר.
- **מסלול חינמי אינו מקבל תזכורת**: הוא אינו פוקע, ואין על מה להתריע.
- **כרטיס תקף** = טוקן שמור + חודש/שנה שלא עברו. שירותי החידוש בודקים
  קיום בלבד ונותנים לסולק לדחות; כאן השאלה היא אם יש למה לצפות.
- **תקרת פיגור** של 30 יום מהשלב האחרון: בלעדיה, ביום הפריסה הראשון כל
  מי שנרשם אי פעם ולא שילם היה מקבל דיוור באותו בוקר.
- מי שפספס שלבים מקבל את **המאוחר** שמתאים למצבו, לא את הראשון.
- שעות אנושיות ולא בשבת, כמו בהזמנה לשיחת ההיכרות.
- הסימון נתפס לפני השליחה (`settings->>'activationNudge:<stage>'`), אטומית.

**שם מסלול השותפים** מגיע מהגדרת הפלטפורמה `partnerPlanCode` ונקרא
מקטלוג המסלולים. ריק או קוד שאינו בקטלוג ⟵ ההודעה אומרת „החשבון ננעל”
בלי לנקוב בשם של מסלול שאינו קיים.

**הסרה** (חוק התקשורת §30א): לכל בעלים טוקן קבוע ב-`activation_nudge_optouts`,
והקישור בתחתית כל תזכורת מוביל ל-`/nudge-optout/<token>`. הדף מבקש אישור
ושולח `POST` — קישור במייל נפתח גם בידי סורקי אבטחה, ו-`GET` שמסיר היה
מסיר אנשים שלא לחצו. ההסרה אידמפוטנטית ומכובדת לתמיד; מי שהסיר יוצא
מרשימת הנמענים **לפני** השליחה.

הטבלה נפרדת מ-`users` בכוונה: פוליסת RLS שחושפת שורה לפי טוקן הייתה
פותחת את כל עמודות המשתמש לטרנזקציה הציבורית, כולל `password_hash` —
ו-RLS אינו יודע להגביל עמודות.

### תפעול התור

- **חיפוש חופשי** על מספר הפנייה, השם, הכתובת, הטלפון, שם המשרד
  וטקסט הפנייה. כל מילה חייבת להימצא (וגם, לא או). מספר נמצא גם
  כשהמקפים אינם במקום.
- **מונה על כל לשונית** — ממתינות / בטיפול / נסגרה / הכול.
- **על השורה עצמה**: מספר, מקור, חומרה (`חוסם עבודה` באדום), סוג,
  שם, משרד, ו„לפני כמה זמן”. התאריך המדויק ב-`title`.
- הרשימה מתרעננת כל 45 שניות כשהלשונית גלויה.
- ‎`Ctrl/⌘+Enter` שולח תשובה; „שליחה וסגירה” מקפלת את הכרטיס.
